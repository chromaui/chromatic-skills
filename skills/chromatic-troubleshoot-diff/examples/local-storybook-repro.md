# Local Storybook Reproduction Example

## Input

A customer says Chromatic captured the wrong button state. They confirm the same state is visible in local Storybook before upload.

## Expected use of the skill

- classify as `VD_LOCAL_RENDER_DRIFT`
- state plainly that the root cause is already present before Chromatic capture
- focus the next step on the story, component, or shared preview code
- avoid framing Chromatic as the primary cause
