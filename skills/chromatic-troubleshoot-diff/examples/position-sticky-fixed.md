# Position Sticky / Fixed Example

## Input

A customer says their sticky footer is pinned incorrectly in Chromatic, and a fixed toolbar sometimes appears blank when the story is captured in isolation.

## Expected use of the skill

- classify as `VD_POSITION_STICKY_OR_FIXED`
- explain that Chromatic captures sticky and fixed elements at their initial positions
- focus the next step on the story wrapper layout, viewport size, and explicit container dimensions
- avoid framing this as a generic animation or asset-loading issue
