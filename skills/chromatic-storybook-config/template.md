# Storybook Config Intake

## Minimal customer request

Please share the exact error message, plus `.storybook/main.*`, `.storybook/preview.*`, and the Storybook-related scripts and dependencies from `package.json`.

## Story discovery request

If the problem is missing stories, also share one story file path that should be detected.

## Local verification request

Please confirm whether local Storybook already fails before Chromatic runs.
