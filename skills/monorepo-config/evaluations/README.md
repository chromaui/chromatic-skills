# Monorepo Config Evaluations

These scenarios verify that the public monorepo-config skill:
- makes an explicit topology decision
- treats Nx and Turborepo as first-class scenarios
- recommends exact snippets when enough context is available
- keeps `untraced` and `externals` recommendations honest about risk
- stays customer-safe
